<UserControl x:Class="TrboX.FastPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:TrboX"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <local:FastPanelTypeConverter x:Key="TypeToColor" />
    </UserControl.Resources>
        <!--Simple Tree View - 这用来在 ScrollViewer 中对 TreeViewItems 进行布局 -->
        
    <Grid>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="30"/>
    		<RowDefinition Height="*"/>
    	</Grid.RowDefinitions>
        <Border Background="{Binding Path = m_Type, Converter= {StaticResource TypeToColor}}" BorderThickness="0"/>
        <!--<Border  BorderThickness="0" Background="#FFCBA3A3"/>-->
    	<DockPanel HorizontalAlignment="Left" LastChildFill="False" VerticalAlignment="Top" Grid.Row="1"/>
    </Grid>
</UserControl>
